# Context capsules

This folder contains the smallest context package needed for one agent task. A capsule lists its purpose, selected inputs, expected output, acceptance criteria and stop conditions.

`weekly_launch_update.md` is the ACME example. It points to the necessary context instead of asking an agent to read the whole repository.
