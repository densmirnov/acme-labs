# Current context

Time-bound inputs for the represented Weekly Launch Update live in [the workflow inputs folder](../../workflows/weekly-launch-update/inputs/).

They currently include the approved launch goal, current-week notes, previous decisions, and customer signals. A future cycle should receive its own preserved inputs; it must not silently reuse a stale “current” folder.

